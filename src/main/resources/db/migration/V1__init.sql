CREATE TABLE mieszkania
(
    id INTEGER PRIMARY KEY,
    developer TEXT,
    investment TEXT,
    number TEXT,
    area NUMERIC,
    price NUMERIC,
    rooms INTEGER,
    lat DOUBLE PRECISION,
    lng DOUBLE PRECISION
);
